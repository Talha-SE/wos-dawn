# Server
PORT=4000
CORS_ORIGIN=http://localhost:5173
JWT_SECRET=please_change_me
ADMIN_SECRET=
ENABLE_CRON=true

# MongoDB Atlas
MONGODB_URI=mongodb+srv://<user>:<pass>@<cluster>/<db>?retryWrites=true&w=majority&appName=<app>

# External APIs (defaults already set)
WOS_PLAYER_URL=https://wos-giftcode-api.centurygame.com/api/player
WOS_GIFT_URL=https://wos-giftcode-api.centurygame.com/api/gift_code
WOS_PLAYER_URL=https://wos-giftcode-api.centurygame.com/api/player
WOS_GIFT_URL=https://wos-giftcode-api.centurygame.com/api/gift_code
ADMIN_SECRET=
WOS_SECRET=tB87#kPtkxqOS2




------------------

cat > .env << 'EOF'
PORT=4000
MONGODB_URI=mongodb+srv://rtalhaonline_db_user:OdQWYvyfe6LHSjmF@cluster0.eyrko9d.mongodb.net/wos-dawn?retryWrites=true&w=majority&appName=Cluster0
JWT_SECRET=change-me
CORS_ORIGIN=https://wos-dawn.vercel.app
ENABLE_CRON=true

# Remove if unused
MISTRAL_API_KEY=15kDmUQWK3DPFNJOAG44mbRe8Liw1mPB
EOF


curl -4 ifconfig.co
# copy the IP printed, then test:
curl -I http://140.238.64.40


# Get your VM public IP
curl -4 ifconfig.co

# Create a small updater
cd ~
mkdir -p duckdns
tee ~/duckdns/wos-dawn.sh >/dev/null <<'SH'
#!/usr/bin/env bash
TOKEN="faa6442d-6ba8-4710-a5b4-c88291e4a982"             # <-- put your DuckDNS token here
DOMAIN="wos-dawn"
IP=$(curl -4 -s ifconfig.co)  # detect current public IP
curl -s "https://www.duckdns.org/update?domains=${DOMAIN}&token=${TOKEN}&ip=${IP}" | tee -a ~/duckdns/duckdns.log
SH
chmod +x ~/duckdns/wos-dawn.sh

# Run once now
~/duckdns/wos-dawn.sh